# Audacity Light Example

This example shows how to use **waveform-playlist** to build a minimal MP3 editor similar to a lightweight DAW. It demonstrates:

- importing a local MP3 file
- selecting regions with mouse drag
- displaying start and end times
- zooming in and out
- basic playback controls

Open `index.html` in a browser to try it.
